---
title: Custom HTTP Requests
description: Learn how to customize HTTP requests with custom headers
---

# Customizing HTTP Requests

You might need to send custom HTTP headers (e.g., `User-Agent`, `Authorization`) to access certain content or interact with APIs.

## Solution

Configure [custom HTTP headers](/ruby-gem/reference/headers) in your feed configuration.
