---
title: "Getting Started"
description: "Run html2rss-web with Docker, paste a page URL, enter the demo token, and open your generated RSS feed."
sidebar:
  order: 1
---

import { Code } from "@astrojs/starlight/components";

import MinimalDockerCompose from "../../components/docs/MinimalDockerCompose.astro";

Run `html2rss-web` locally with Docker, create one feed from a page URL, then move to production setup when you want stable hosting.

## What you need

- Docker
- About two minutes for first success

## Step 1: Folder and compose file

<Code
  code={`
  mkdir html2rss-web && \\
  cd html2rss-web
`}
  lang="bash"
/>

Use the quickstart stack from [`docker-compose.quickstart.yml`](https://github.com/html2rss/html2rss-web/blob/main/docker-compose.quickstart.yml). Save it as `docker-compose.yml`:

<MinimalDockerCompose />

That stack runs:

- `html2rss-web` on port `4000`
- companion `botasaurus` for JavaScript-rendered pages
- `AUTO_SOURCE_ENABLED=true` so URL-to-RSS works without YAML
- demo access token `CHANGE_ME_ADMIN_TOKEN`

## Step 2: Start

<Code code={`docker compose up -d`} lang="bash" />

## Step 3: Create a feed

1. Open `http://localhost:4000` (or `http://<LAN_IP>:4000/` on your network).
2. Paste a **listing** URL — newsroom, changelog, releases, category, or archive page (not a bare homepage).
3. When prompted, enter `CHANGE_ME_ADMIN_TOKEN`.
4. Open the generated feed URL (`.rss` or `.json`).

## Next

1. **[Deployment](/web-application/deployment/)** — production compose, real tokens, LAN HTTP vs HTTPS proxy.
2. **[Feed Directory](/web-application/guides/use-the-feed-directory/)** — subscribe when a curated feed already covers the site.
3. **[Advanced Feeds](/creating-custom-feeds/)** — custom YAML only when auto-source needs tighter selectors.

## Ruby CLI (optional)

Without the web UI, one-shot auto-source from the gem:

<Code code={`html2rss scrape https://example.com/blog`} lang="bash" />

`html2rss auto` is a Thor alias for `scrape`. Strategy details: [Strategy reference](/ruby-gem/reference/strategy/).
