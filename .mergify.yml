queue_rules:
  - name: default
    conditions:
      - author=dependabot[bot]

pull_request_rules:
  - name: automatic merge for Dependabot pull requests
    conditions:
      - author=dependabot[bot]
    actions:
      merge:
        method: squash
      queue:
        method: merge
        name: default
